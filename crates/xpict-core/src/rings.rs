//! SSSR helpers and ring-interior normals.
//!
//! **Stub.** Python still owns `draw/rings.py`. Port after the drawer geometry
//! is stable; do not invent ring-placement rules here (layout backends own
//! coordinates — see `docs/layout-notes.md`).

#![allow(dead_code)]

/// Placeholder so the module path exists for incremental moves.
#[derive(Debug, Clone, Copy, Default)]
pub struct RingsCore;
